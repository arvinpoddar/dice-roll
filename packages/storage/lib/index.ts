import { createStorage, StorageType, type BaseStorage, SessionAccessLevel } from './base';
import { schemaStorage } from './schemaStorage';

export { schemaStorage, createStorage, StorageType, SessionAccessLevel, BaseStorage };
